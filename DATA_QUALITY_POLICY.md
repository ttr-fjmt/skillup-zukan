# データ品質ポリシー

このリポジトリでデータを扱うときの判断基準。新しいジャンルを追加するたびに、ここに書かれた
ルールを毎回適用する。

## 大原則

**ページ本文に書かれていないことは、絶対に作らない。**

これまで見つかった問題は、すべて「AIがもっともらしいが本文に無い情報を作ってしまう」という
同じパターンだった。

| いつ | 何が起きたか |
| --- | --- |
| programming 初回 | フッターの `© 2026 Brewus,Inc.` から「株式会社Brewus」を合成（正しくは株式会社ブリューアス）。3件中3件で発生 |
| programming 初回 | 「転職成功率99%」「10万人以上の受講生を輩出」を中立的な特徴として掲載 |
| area フォールバック初回 | 会社概要ページの**本社住所**を通学拠点として採用 |
| price 移行 | 割引後価格を「最安値」として採用（475,200円 → 456,390円） |
| video_editing 初回 | 都道府県24件のうち11件、`career_paths` 7件のうち3件が本文に無い |
| video_editing 初回 | 月額39,600円を一括475,200円と同じ `min_yen` 軸に並べた |
| 日次cron | 「英語表記のみなら null」がプロンプトにしか無く、`CodeCamp Co., Ltd.` が通過 |

新しいフィールドを扱うときは、常に「この値は本文のどこに書いてあるか、機械的に照合できるか」
を自問する。照合できないなら値を確定させない。

## 確認できない場合のルール

- 値を無理に埋めない。空欄（`null` / `[]`）にする
- なぜ確認できなかったかを `review_flags` に記録する（`area_unconfirmed` / `format_unconfirmed` /
  `price_scope_limited` / `detail_page_crawled`）
- **掲載は止めない。** 承認フェーズは存在しないため、確認できない状態のまま掲載を続け、
  後から見つけて直せるようにする

## 代表値を選ぶときの注意

「一番安い金額」「一番短い期間」のように複数の値から1つを選ぶ処理が、最も事故が起きやすい。
実際に起きた誤りは上表のとおり。**迷う場合は「代表値を作らない」を優先する。**

現状の判断:

- `price.min_yen` は同じ `kind`（total / monthly）の中でのみ求める。入学金は使わない
- 期間の代表値は**作らない**。`plans[].duration` としてプラン単位でのみ持つ
  （sejuku の「無料カウンセリング実施後2週間以内のご入会」を受講期間として掲げかけた）
- 同一ラベルで複数の金額があれば、高い方（通常価格）を採る

## 新しいフィールドを扱うときの手順

1. まずページ本文を実際に読み、そのフィールドの情報がどこにどう書かれているか把握する
2. AIに抽出させた後、**抽出結果が本文に実在するか機械的に照合するガードを必ず用意する**。
   自由記述系フィールドはすべて必要。ガードを付け忘れたフィールドで毎回問題が再発している
3. 新しいジャンルを1つ追加するたびに、**追加済みの全ジャンル・全校**に対して同じガードを
   再チェックする（`node recheck-guards.js`）。あるジャンルで見つかった問題は、既存の
   他ジャンルにも隠れている可能性が高い

## 掲載対象の定義

**自社で講座を提供しているスクールだけを掲載する。** 他社・個人の講座を集めた
ポータル・講座検索サイト・マーケットプレイスは対象外（`scraper/lib/portal-filter.js`）。

講座ごとに料金も期間も違うため、この図鑑の比較軸（料金・期間・レベル）がまるごと埋まらない。
実際にマナビDX（経産省・IPAの講座検索ポータル）とストアカ（個人講師が出品するマーケット
プレイス、約37,000講座）が掲載され、どちらも料金・期間が空欄になった。

判定キーワードは「第三者が講座を出す場であること」を示す語だけを使い、掲載中の実データで
較正してある（スクール17件に1件もヒットしないことを実測。「講座を探す」は実在のスクールにも
ヒットしたため不採用）。

## 現在のガード一覧

| ガード | 対象 |
| --- | --- |
| `verifyCandidate()` | スクールの実在（HTTP照合） |
| `verifyOfficialName()` | 運営会社名（本文照合＋英語表記のみを排除） |
| `verifyCareerPaths()` | 職種名 |
| `verifyPrefectures()` / `filterToCampusPrefectures()` | 都道府県（本文照合＋通学拠点の根拠） |
| `verifyPlans()` / `amountAppearsAsYen()` | 金額・期間（円の印があること・桁の一部一致を排除） |
| `dropDiscountedDuplicates()` | 割引価格の混入 |
| `buildPriceFromPlans()` | 種別をまたぐ金額比較の防止 |
| `verifySubsidyClaim()` | 給付金対象 |
| `filterFeatures()` / `stripExaggeratedSentences()` | 誇張・実績訴求 |
| `classifyFormat()` | 受講形式 |
| `verifyGenres()` | ジャンル（本文に根拠があるか） |
| `portalMarkers()` / `agencyScore()` | 掲載対象（ポータル・代行会社の除外） |
| `buildSchoolLd()` ほか（`lib/structured-data.js`） | 構造化データ（レコードに無い値・月額を一括料金として出さない） |
| `buildFaqLd()`（`build-faq-jsonld.js`） | よくある質問の構造化データ（本文から生成） |

## 人間に確認すること

- これまでのルールで対処できない、**新しい種類の曖昧さ**が出たとき
- **データ構造そのものを変える**必要がある設計判断
- **お金・法律・利用規約**に関わる判断（スクレイピング対象の追加、口コミ収集の開始可否など）
- 一度決めたら**後から直すコストが高い**決定

## 人間に確認せず進めてよいこと

- 既に決まっているルールを、新しいジャンル・新しいレコードに適用すること
- 過去に見つかったのと同じパターンの問題を、同じ方法で修正すること
- テストの追加、ガードの追加など、品質を担保するための実装
- 明らかなバグ修正（IDの重複、既存ルールとの矛盾など）

## ジャンル展開の進め方

1ジャンルにつき `max_candidates=3` 程度の小規模実行から始める。このポリシーに沿って処理し、
「人間に確認すること」に該当する問題が出た場合のみ止めて報告する。問題がなければそのまま
次のジャンルに進む。

全8ジャンルの初回レビューは完了済み。日次cronは全8ジャンルを対象にしており、
上限件数で打ち切られてもどのジャンルにも順番が回るよう、並び順を日付で回転させている。
